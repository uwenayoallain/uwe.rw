---

---

<section id='about' class='section about'>
    <div class='container'>
        <div class='about-grid'>
            <div class='about-content fade-up'>
                <h2 class='section-title'>Beyond Visuals</h2>
                <p class='about-text'>
                    I don't just build websites; I create digital experiences
                    that resonate. With a background in both design and
                    engineering, I bridge the gap between aesthetics and
                    functionality. My philosophy is simple: make it beautiful,
                    make it fast, and make it work.
                </p>
                <p class='about-text'>
                    Every pixel is placed with purpose, every interaction is
                    crafted with care. I believe in the power of minimalism and
                    the impact of bold typography.
                </p>
                <a href='/about' class='about-link magnetic-link'
                    >More About Me</a
                >
            </div>

            <div class='about-stats fade-up'>
                <div class='stat-item'>
                    <span class='stat-number'>3+</span>
                    <span class='stat-label'>Years Experience</span>
                </div>
                <div class='stat-item'>
                    <span class='stat-number'>20+</span>
                    <span class='stat-label'>Projects Delivered</span>
                </div>
                <div class='stat-item'>
                    <span class='stat-number'>100%</span>
                    <span class='stat-label'>Client Satisfaction</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .about {
        padding: var(--space-3xl) 0;
    }

    .about-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--space-2xl);
        align-items: start;
    }

    .section-title {
        font-size: var(--text-3xl);
        font-weight: 700;
        margin-bottom: var(--space-md);
        line-height: 1.1;
    }

    .about-text {
        font-size: var(--text-lg);
        color: var(--color-text-muted);
        margin-bottom: var(--space-md);
        line-height: 1.6;
    }

    .about-link {
        display: inline-block;
        font-size: var(--text-base);
        color: var(--color-text);
        text-decoration: underline;
        text-underline-offset: 4px;
        margin-top: var(--space-sm);
    }

    .about-stats {
        display: grid;
        grid-template-columns: 1fr;
        gap: var(--space-xl);
        padding-left: var(--space-2xl);
        border-left: 1px solid var(--color-border);
    }

    .stat-item {
        display: flex;
        flex-direction: column;
    }

    .stat-number {
        font-size: var(--text-3xl);
        font-weight: 700;
        color: var(--color-accent);
        line-height: 1;
        margin-bottom: var(--space-xs);
    }

    .stat-label {
        font-family: var(--font-mono);
        font-size: var(--text-sm);
        color: var(--color-text-muted);
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    @media (max-width: 900px) {
        .about-grid {
            grid-template-columns: 1fr;
            gap: var(--space-xl);
        }

        .about-stats {
            padding-left: 0;
            border-left: none;
            border-top: 1px solid var(--color-border);
            padding-top: var(--space-xl);
            grid-template-columns: repeat(3, 1fr);
            gap: var(--space-md);
        }
    }

    @media (max-width: 600px) {
        .about-stats {
            grid-template-columns: 1fr;
        }
    }
</style>
